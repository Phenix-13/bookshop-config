server:
  port: 8080

spring:
  application:
    name: library
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/librarydb_test?createDatabaseIfNotExist=true
    username: root
    password: P@$$w0rd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: optional:configserver:http://localhost:8071/

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka/
